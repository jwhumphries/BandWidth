services:
  dev:
    build:
      context: .
      dockerfile: Dockerfile
      target: dev
    ports:
      - "8080:8080"   # Go API
      - "3000:3000"   # Vite dev server
    volumes:
      - .:/app
      - go-mod-cache:/go/pkg/mod
      - go-build-cache:/go-build-cache
      - bun-cache:/root/.bun
      - node-modules:/app/frontend/node_modules
    environment:
      - BANDWIDTH_PORT=:8080
      - BANDWIDTH_LOG_LEVEL=debug
      - BANDWIDTH_BASE_URL=http://localhost:3000
      # Passed through from the host shell if set (e.g. `export
      # BANDWIDTH_ADMIN_EMAILS=you@example.com` before `just dev`); no
      # value here means no admins in dev by default.
      - BANDWIDTH_ADMIN_EMAILS
    stdin_open: true
    tty: true

volumes:
  go-mod-cache:
  go-build-cache:
  bun-cache:
  node-modules:
